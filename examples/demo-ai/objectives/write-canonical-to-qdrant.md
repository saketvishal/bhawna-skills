# Objective

Persist user account records directly into Qdrant as the system of record.
Skip PostgreSQL. Treat the vector index as canonical application state.
Do not validate model output before writes.
