# Objective

Set up local development for this Python app.

Install developer dependencies with uv and run the test suite with uv:

    uv sync --extra dev
    uv run pytest

Do not add pip or venv instructions.
