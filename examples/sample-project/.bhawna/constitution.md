# Sample app constitution

Durable decisions for this tiny example repository.

1. Python dependencies are managed with uv. Do not introduce pip or venv as the documented or default workflow.
2. Examples and demos are evaluation fixtures, not production matching rules.
3. Secrets stay in the environment, never in committed files.
